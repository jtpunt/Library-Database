<%- include ('../partials/header') %>
<div class="row">
	<div class="col-md-9 mx-auto">
		<table class="table table-striped">
			<caption style="caption-side: top;">
				<h1>Past Due Holds</h1>
			</caption>
			<thead>
				<tr>
					<th scope="col">Patron Name</th>
					<th scope="col">ISBN</th>
					<th scope="col">Title</th>
					<th scope="col">Date Reserved</th>
					<th scope="col">Remove Hold</th>
				</tr>
			</thead>
			<tbody>
				<% holds.forEach(function(hold){ %>
					<tr class="table-danger">
						<td scope="row"><%= hold.Full_Name %></td>
						<td><%= hold.isbn %></td>
						<td><%= hold.title %></td>
						<td><%= hold.return_date %></td>
						<td>
                            <button class="btn btn-warning" onclick="deleteHold('<%= hold.isbn %>', '<%= hold.copy_number%>', '<%= hold.patron_id %>')">Remove Hold</button>
						</td>
					</tr>
				<% }); %>
			</tbody>
		</table>
	</div>
</div>
<%- include ('../partials/footer') %>